#include <iostream>

// This is a dummy test file that doesn't rely on GTest
// It will be used just to make sure the build system can compile test files
 
int main() {
    std::cout << "Dummy test compiled and ran successfully!" << std::endl;
    return 0;
} 